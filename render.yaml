services:
  - type: web
    name: ai-chatbot
    env: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: PORT
        value: 3000